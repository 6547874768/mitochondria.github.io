name: Auto-generate Sitemap

# Запускается при каждом push в main ветку
on:
  push:
    branches: [ main, master ]
  # Также можно запустить вручную
  workflow_dispatch:

jobs:
  generate-sitemap:
    runs-on: ubuntu-latest
    
    # Права для записи в репозиторий
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # Получаем полную историю для корректной работы
        fetch-depth: 0
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Create sitemap generator script
      run: |
        cat > generate_sitemap.py << 'EOF'
        #!/usr/bin/env python3
        """
        Автоматический генератор sitemap.xml для GitHub Pages
        """
        
        import os
        import datetime
        from pathlib import Path
        import re
        
        # Конфигурация
        SITE_URL = "https://www.danilichev.info"
        EXCLUDE_DIRS = ['.git', '.github', 'node_modules', '__pycache__']
        EXCLUDE_FILES = ['README.md', 'sitemap.xml', 'robots.txt', '.gitignore', 'CNAME', '404.html']
        
        def scan_html_files():
            """Сканирует проект и находит все HTML страницы"""
            urls = []
            today = datetime.date.today().strftime('%Y-%m-%d')
            
            # Добавляем главную страницу
            if os.path.exists('index.html'):
                urls.append({
                    'loc': SITE_URL + '/',
                    'lastmod': today,
                    'changefreq': 'weekly',
                    'priority': '1.0'
                })
            
            # Сканируем директории для поиска папок с index.html
            for root, dirs, files in os.walk('.'):
                # Исключаем ненужные директории
                dirs[:] = [d for d in dirs if not any(exclude in d for exclude in EXCLUDE_DIRS)]
                
                # Пропускаем корневую директорию (уже обработана выше)
                if root == '.':
                    continue
                
                # Проверяем наличие index.html в директории
                if 'index.html' in files:
                    # Формируем URL из имени директории
                    dir_path = os.path.relpath(root, '.')
                    url_path = '/' + dir_path.replace('\\', '/') + '/'
                    
                    # Определяем приоритет и частоту обновления
                    priority = determine_priority(url_path)
                    changefreq = determine_changefreq(url_path)
                    
                    urls.append({
                        'loc': SITE_URL + url_path,
                        'lastmod': today,
                        'changefreq': changefreq,
                        'priority': priority
                    })
            
            return sorted(urls, key=lambda x: float(x['priority']), reverse=True)
        
        def determine_priority(url_path):
            """Определяет приоритет страницы на основе URL"""
            # Главная страница
            if url_path == '/':
                return '1.0'
            
            # Основной продуктовый лендинг
            if 'advanced-mitochondrial-formula' in url_path:
                return '0.95'
            
            # Категории (основные разделы)
            categories = [
                'health-supplements', 'health-products', 'anti-aging-hacks', 
                'brain-supplements', 'weight-loss-supplements'
            ]
            if any(cat in url_path for cat in categories):
                return '0.9'
            
            # Информационные страницы
            return '0.8'
        
        def determine_changefreq(url_path):
            """Определяет частоту обновления"""
            # Главная и категории обновляются чаще
            if url_path == '/' or float(determine_priority(url_path)) >= 0.9:
                return 'weekly'
            return 'monthly'
        
        def generate_sitemap_xml(urls):
            """Генерирует XML содержимое sitemap"""
            xml_content = '''<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9
        http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd">
'''
            
            for url in urls:
                xml_content += f'''
    <url>
        <loc>{url['loc']}</loc>
        <lastmod>{url['lastmod']}</lastmod>
        <changefreq>{url['changefreq']}</changefreq>
        <priority>{url['priority']}</priority>
    </url>'''
            
            xml_content += '''

</urlset>'''
            
            return xml_content
        
        def main():
            """Основная функция генерации sitemap"""
            print("🔍 Сканирование директорий с HTML файлами...")
            urls = scan_html_files()
            
            print(f"📄 Найдено {len(urls)} страниц:")
            for url in urls:
                print(f"  - {url['loc']} (приоритет: {url['priority']})")
            
            print("\n📝 Генерация sitemap.xml...")
            sitemap_content = generate_sitemap_xml(urls)
            
            # Сохраняем файл
            with open('sitemap.xml', 'w', encoding='utf-8') as f:
                f.write(sitemap_content)
            
            print("✅ Sitemap успешно сгенерирован!")
            print(f"🌐 URL: {SITE_URL}/sitemap.xml")
        
        if __name__ == "__main__":
            main()
        EOF
    
    - name: Generate sitemap
      run: |
        echo "🚀 Запуск генератора sitemap..."
        python generate_sitemap.py
        
    - name: Check if sitemap changed
      id: check_changes
      run: |
        if git diff --quiet sitemap.xml; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "📋 Sitemap не изменился"
        else
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "🔄 Sitemap обновлен"
        fi
    
    - name: Commit and push sitemap
      if: steps.check_changes.outputs.changed == 'true'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions Bot"
        git add sitemap.xml
        git commit -m "🤖 Auto-update sitemap.xml - $(date '+%Y-%m-%d %H:%M:%S')"
        git push
        echo "✅ Sitemap committed and pushed!"
    
    - name: No changes message
      if: steps.check_changes.outputs.changed == 'false'
      run: |
        echo "ℹ️ Sitemap не требует обновления"
